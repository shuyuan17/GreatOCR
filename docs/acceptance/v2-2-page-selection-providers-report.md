# GreatOCR V2.2 页面选择与多模型架构验收报告

日期：2026-07-01  
分支：`codex/v2-2-page-selection-providers`

## 阶段结论

V2.2 已完成离线自动化验收和一份经用户明确授权的真实 MinerU 样本验收。本阶段建立了页面选择、仅发送选中页面、供应商能力卡、通用多模态实验适配器、可选文本校正、失败切换策略、页面返工和结果版本机制。

## 已验收能力

- 支持 `3, 8-10, 9, 1` 等页码表达式，并校验空值、倒序、越界和非法字符。
- 生成只包含选中页面的临时子 PDF；保存任务页码到原 PDF 页码的映射。
- 中断恢复后可以从任务清单恢复页码映射。
- 默认合并选中页；按区间输出时生成 `result-pages-<范围>.docx`。
- 供应商注册表按文字、坐标、版面、表格、图片和公式能力筛选方案。
- 通用多模态实验适配器严格校验 JSON Schema、页码、置信度和归一化坐标。
- 智能文本校正默认关闭；失败时继续生成 Word；关键字段自动修改默认拒绝。
- 失败切换默认要求用户选择；自动模式只使用任务开始前批准且能力满足要求的方案。
- 任务清单记录批准的 profile ID 和确认时间，不保存 API Key。
- 页面返工只发送指定失败页；成功后替换对应页并保留其他页面稳定 ID。
- 首次结果保存为 `result-v1.docx`；返工继续生成 `result-v2.docx` 等版本，同时更新 `result.docx`。
- 新 DOCX 通过包结构验证后才发布为当前结果。

## 验证记录

- 全量测试：`.venv\Scripts\python.exe -m pytest -q`
- 结果：165 项通过，0 项失败。
- 离线端到端：`.venv\Scripts\python.exe scripts\run_acceptance.py --provider fake`
- 结果：`fake provider acceptance passed`。

## 安全与外部服务说明

本阶段仅对用户明确指定并授权的 `英文签字盖章有页眉的.pdf` 执行一次真实 MinerU 上传。密钥通过 Windows 环境变量提供；没有读取项目中的 API Key 文件，输出文件中未检出密钥。没有调用千问、Kimi、豆包、DeepSeek 或其他线上模型，也没有向 GitHub 上传代码。

## 真实 MinerU 样本验证

- 授权样本：`testsamples/英文签字盖章有页眉的.pdf`，共 3 页。
- MinerU 返回 3 页解析结果，完整链路生成 `result.docx` 和 `quality-report.docx`。
- 两个 DOCX 均通过包结构验证；任务记录 5 项质量问题，供后续目视验收参考。
- 处理前后源 PDF SHA-256 保持一致。
- 本地输出目录未检出 `MINERU_API_KEY` 内容。
- 输出目录：`outputs/mineru-v2-2-header-signature-20260701-142935`。

## 本地真实 PDF 样本验证

使用 `testsamples/` 中的 9 份真实 PDF 完成纯本地预检：

- `中英夹杂的.pdf`：2 页扫描件，两页旋转值均为 270°；渲染后页面方向正确。
- `30页中文.pdf`：30 页，其中第 5、6、7、8、30 页识别为扫描页。
- `超长160多页中文.pdf`：实际 192 页；仅抽取第 1、96、192 页成功，原文件未被修改。
- `中文但有水印的.pdf`：识别为加密 PDF，页数不在未解密状态下继续解析。
- 盖章、英文表格、长文档第 96 页和扫描财务表页面均完成 PNG 目视检查，未发现抽页导致的裁切、旋转或内容缺失。

选页合同验证覆盖以下映射，所有源文件 SHA-256 在处理前后保持一致：

- 30 页中文：任务页 1、2、3 对应原页 1、5、30。
- 中英旋转件：任务页 1、2 对应原页 1、2。
- 中文盖章件：任务页 1、2 对应原页 1、3。
- 英文表格件：任务页 1 对应原页 2。
- 192 页长文档：任务页 1、2、3 对应原页 1、96、192。

另使用 fake provider 对旋转件第 2 页、盖章件第 3 页、表格件第 2 页和长文档第 192 页运行完整离线链路。四项均恢复正确原页码，生成 `result-v1.docx`，且 DOCX 包结构验证通过。

以上本地验证证明真实 PDF 的预检、选页、页码映射、Word 生成和版本发布链路有效；另有一份经明确授权的样本完成真实 MinerU 解析。OCR 文字、签字盖章、页眉和版面质量仍需结合生成的 Word 进行目视验收。

## 下一阶段边界

V2.3 将在这些后端能力之上建设 FastAPI + React 本机图形界面、任务历史、设置中心、Windows 当前用户凭据、串行队列和敏感文件二次确认页面。任何新增真实样本上传仍需用户逐次明确批准。
